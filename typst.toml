[package]
name = "mailmerge"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Aurghya & Antigravity"]
license = "MIT"
description = "A powerful, feature-rich mail merge package for Typst. Easily generate personalized letters, certificates, address labels, badges, envelopes, and invoices from CSV data."
repository = "https://github.com/typst/packages"
keywords = ["mail-merge", "csv", "templates", "labels", "certificates", "letters", "reports", "envelopes"]
compiler = "0.11.0"
categories = ["utility", "layout"]
